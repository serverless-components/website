{
  "name": "@serverless/website-src",
  "version": "1.0.0",
  "main": "./serverless.js",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint . --fix --cache"
  },
  "author": "Serverless, Inc.",
  "license": "Apache",
  "dependencies": {
    "@serverless/aws-sdk-extra": "^0.1.0",
    "klaw-sync": "^6.0.0",
    "mime-types": "^2.1.22",
    "parse-domain": "^3.0.2"
  }
}
